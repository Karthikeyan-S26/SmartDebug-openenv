[project]
name = "smartdebug-env"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic<2.10",
    "openenv-core>=0.2.0",
    "requests"
]

[project.scripts]
server = "server.app:main"
